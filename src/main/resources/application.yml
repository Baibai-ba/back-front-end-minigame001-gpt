server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/minigame001?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
    username: root        # 改成你的本地账号
    password: 1234   # 改成你的本地密码
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.hongbin.minigame001.domain